---
"@maple-kit/mcp": minor
---

`get_comment_context` returns `mock: { recipe, replay }` for a comment written
under a Maple Mock, where `replay` opens the comment's page with the recipe on,
and adds a `mocked: …` line to `conditions`.
