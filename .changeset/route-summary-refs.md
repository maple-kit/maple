---
"@maple-kit/core": patch
---

`/mock/plan` describes a call's schema past a local `$ref`, naming the
component it points at (`User: id, name, since`), and no longer repeats names
the page already sent.
