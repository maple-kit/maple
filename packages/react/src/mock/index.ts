/**
 * React bindings for Maple Mock's box. Its own subpath, so an application that
 * only reviews never loads the mock controller.
 */

export { useMock } from "./use-mock.js";
export type { MockBinding, UseMockOptions } from "./use-mock.js";
