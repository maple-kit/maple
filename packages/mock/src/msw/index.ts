/**
 * The opt-in MSW transport, for a host that already runs `setupWorker`.
 *
 * Empty until the codecs exist: its handlers are those codecs behind MSW's
 * request handler, so there is nothing to wrap yet.
 */

export {};
