/**
 * The same handlers as `@maple-kit/mock/msw`, for `setupServer` in Vitest,
 * Playwright or a server render.
 *
 * Empty until the codecs exist, for the same reason as that entry.
 */

export {};
