# Extends the bundled rule set rather than replacing it, so new upstream rules
# arrive with a gitleaks upgrade instead of needing a copy here.
[extend]
useDefault = true

[[rules]]
id = "maple-github-app-private-key"
description = "GitHub App private key (PEM)"
regex = '''-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----'''
keywords = ["PRIVATE KEY"]

# For the Datadog connector in docs/connectors.md. An app key is not in
# gitleaks' default rule set and is what its observability path would need.
[[rules]]
id = "maple-datadog-app-key"
description = "Datadog application key"
regex = '''(?i)\b(dd|datadog)[_-]?app(lication)?[_-]?key\b\s*[:=]\s*['"]?([a-f0-9]{40})['"]?'''
keywords = ["datadog", "dd_app"]

[allowlist]
description = "Files that hold credential names and integrity hashes, never a value"
paths = [
  '''\.env\.example$''',
  '''pnpm-lock\.yaml$''',
]
